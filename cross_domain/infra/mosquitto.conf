# Mosquitto MQTT broker — Soil + Plant + Biotic Pod DT System
listener 1883
allow_anonymous true

# WebSocket listener (browser dashboards / MQTT over WebSocket)
listener 9001
protocol websockets
allow_anonymous true

# Logging
log_type error
log_type warning
log_type notice
log_type information
